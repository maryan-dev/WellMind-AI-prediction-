# Public FastAPI URL (no trailing slash). Required on Vercel — the dev proxy does not run in production.
# Example after you deploy API to Render/Railway/Fly:
# VITE_API_URL=https://your-wellmind-api.onrender.com

VITE_API_URL=
