# Vercel deploy (WellMind frontend only)

1. Import https://github.com/maryan-dev/WellMind-AI-prediction-
2. **Root Directory:** `frontend`
3. **Framework:** Vite (auto)
4. **Do not** add Python / Serverless
5. Env (optional): `VITE_API_URL` = your FastAPI URL
6. Deploy

There must be **no** `vercel.json` in the repo root (only this folder’s `vercel.json`).
